spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/lin_xin?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: venus@911NASA
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration/mysql
    encoding: UTF-8

auth:
  secret: linXinMgrSecretKey2024ForJwtTokenGeneration
  expire: 1440

logging:
  level:
    org.venus.lin.xin.mgr: debug
